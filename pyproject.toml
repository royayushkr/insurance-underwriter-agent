[project]
name = "insurance-underwriter-agent"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.115.11",
    "uvicorn[standard]==0.34.0",
    "pydantic==2.12.5",
    "httpx",
    "python-dotenv",
]

[tool.vercel]
entrypoint = "backend.app:app"
